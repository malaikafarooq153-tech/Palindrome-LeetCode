# Palindrome-LeetCode
This program uses Object-Oriented Programming (OOP) in C++ to check if a number is a palindrome. It reverses the given number and compares it with the original value. If both are equal, the number is a palindrome; otherwise, it is not.
